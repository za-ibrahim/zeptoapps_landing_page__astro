---
import { Image } from "astro:assets";
import ComponentLayout from "../../layouts/ComponentLayout.astro";
import RightSide from "./RightSide.astro";
import img_p1 from "../../assets/hero_p1.webp";
import img_p2 from "../../assets/hero_p2.webp";
import img_p3 from "../../assets/hero_p3.webp";
import shopify_partner from "../../assets/shopify-partner.png";
// import shopify_plus from "../../assets/shopify_and_plus.svg";

import logo_1 from "../../assets/hero/_1.webp";
import logo_2 from "../../assets/hero/_2.webp";
import logo_3 from "../../assets/hero/_3.webp";
import logo_4 from "../../assets/hero/_4.webp";
import logo_5 from "../../assets/hero/_5.webp";
import logo_bg from "../../assets/hero/logo_bg.webp";

const { tag } = Astro.props;
---

<div class="main">
  <ComponentLayout>
    <section class="hero-section">
      <div class="hero-container">
        <!-- Left side - Content -->
        <div class="hero-content">
          <!-- <div class="hero-badge">
        <span class="badge-text">🚀 Leading SaaS Solutions</span>
      </div> -->

          <h1 class="hero-title xl:text-7xl lg:text-6xl md:text-5xl text-4xl">
            Build Your Business <span class="xs:inline sm:block lg:inline"
              >with <span class="text-primary">Zepto</span>
              <span class="text-secondary_2">Apps</span></span
            >
          </h1>

          <p class="hero-description md:text-xl text-lg max-w-xl">
            Take your eCommerce store to new heights and accelerate your growth
            like never before with the power of our cutting-edge shopify apps
          </p>

          <div class="hero-buttons">
            <Image
              class="h-10 w-auto"
              src={shopify_partner}
              alt="shopify logo"
            />
            <a href="#products-showcase">
              <button class="learn-more">
                <span class="circle" aria-hidden="true">
                  <span class="icon arrow"></span>
                </span>
                <span class="button-text">Browse Apps</span>
              </button>
            </a>

            <!-- <a
              href="/contact"
              class="flex items-center justify-center ring-2 rounded-full px-8 py-2 font-bold tracking-wide hover:bg-secondary_2 hover:text-white active:scale-100 hover:scale-110 transition-all duration-300 ease-in-out"
            >
              <button class="">
                <span class="">Let's talk</span>
              </button>
            </a> -->
          </div>

          <!-- <div class="hero-stats">
            <div class="stat-item">
              <span class="stat-number">100K+</span>
              <span class="stat-label">Installs</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">99.9%</span>
              <span class="stat-label">Uptime</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">24/7</span>
              <span class="stat-label">Support</span>
            </div>
          </div> -->
        </div>

        <!-- Right side - Animated Products -->
        {
          tag === "1" ? (
            <div class="mx-auto hidden lg:inline-flex">
              <RightSide />
            </div>
          ) : tag === "2" ? (
            <div class="_hero-visual relative hidden lg:inline-flex">
              <Image
                width={800}
                class="h-[580px] w-auto object-contain z-10"
                src={logo_bg}
                alt="Hero Visual"
              />
              <div class="z-20">
                <a href="" class="product-card card-1">
                  <div class="card-icon !w-[85px] h-auto">
                    <Image
                      width={200}
                      src={logo_2}
                      alt="zepto product personalizer"
                    />
                  </div>
                </a>
                <a href="" class="product-card card-2">
                  <div class="card-icon">
                    <Image
                      width={200}
                      src={logo_1}
                      alt="zepto sign customizer"
                    />
                  </div>
                </a>
                <a href="" class="product-card card-3">
                  <div class="card-icon !w-[60px] h-auto">
                    <Image width={200} src={logo_3} alt="zepto infinSEO" />
                  </div>
                </a>
                <a href="" class="product-card card-4">
                  <div class="card-icon">
                    <Image width={200} src={logo_4} alt="bootsly" />
                  </div>
                </a>
                <a href="" class="product-card card-5">
                  <div class="card-icon !w-[75px] h-auto">
                    <Image width={200} src={logo_5} alt="zepto popman" />
                  </div>
                </a>
              </div>
            </div>
          ) : null
        }
      </div>
    </section>
  </ComponentLayout>
</div>

<style>
  /* button style */
  .hero-buttons {
    display: flex;
    gap: 2.5rem;
    margin-bottom: 2rem;
    animation: fadeInUp 0.8s ease-out 0.6s both;
  }
  button {
    position: relative;
    display: inline-block;
    cursor: pointer;
    outline: none;
    border: 0;
    vertical-align: middle;
    text-decoration: none;
    background: transparent;
    padding: 0;
    font-size: inherit;
    font-family: inherit;
  }
  button.learn-more {
    width: 12rem;
    height: auto;
  }
  button.learn-more .circle {
    transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
    position: relative;
    display: block;
    margin: 0;
    width: 3rem;
    height: 3rem;
    background: #282936;
    border-radius: 1.625rem;
  }
  button.learn-more .circle .icon {
    transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
    position: absolute;
    top: 0;
    bottom: 0;
    margin: auto;
    background: #fff;
  }
  button.learn-more .circle .icon.arrow {
    position: absolute;
    transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
    top: -0.1rem;
    left: 0.625rem;
    width: 1.125rem;
    height: 0.15rem;
    background: none;
  }
  button.learn-more .circle .icon.arrow::before {
    position: absolute;
    content: "";
    top: -0.23rem !important;
    right: 0.0625rem;
    width: 0.625rem;
    height: 0.625rem;
    border-top: 0.125rem solid #fff;
    border-right: 0.125rem solid #fff;
    transform: rotate(45deg);
  }
  button.learn-more .button-text {
    transition: all 0.45s cubic-bezier(0.65, 0, 0.076, 1);
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    padding: 0.75rem 0;
    margin: 0 0 0 1.85rem;
    color: #282936;
    font-weight: 700;
    text-align: center;
  }
  button:hover .circle {
    width: 100%;
  }
  button:hover .circle .icon.arrow {
    background: #fff;
    transform: translate(1rem, 0);
  }
  button:hover .button-text {
    color: #fff;
  }

  /* others */
  .hero-section {
    min-height: calc(100vh - 58px - 1rem);
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  /* .hero-section::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      animation: backgroundMove 20s ease-in-out infinite;
    } */

  @keyframes backgroundMove {
    0%,
    100% {
      transform: translateX(0) translateY(0);
    }
    25% {
      transform: translateX(-10px) translateY(-10px);
    }
    50% {
      transform: translateX(10px) translateY(-5px);
    }
    75% {
      transform: translateX(-5px) translateY(10px);
    }
  }

  .hero-container {
    /* max-width: 1200px; */
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-content {
    color: black;
  }

  .hero-badge {
    display: inline-block;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    padding: 0.5rem 1rem;
    margin-bottom: 1.5rem;
    backdrop-filter: blur(10px);
    animation: fadeInUp 0.8s ease-out;
  }

  .badge-text {
    font-size: 0.9rem;
    font-weight: 500;
  }

  .hero-title {
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .gradient-text {
    background: linear-gradient(45deg, #00f5ff, #ff00ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 3s ease-in-out infinite;
  }

  @keyframes gradientShift {
    0%,
    100% {
      filter: hue-rotate(0deg);
    }
    50% {
      filter: hue-rotate(45deg);
    }
  }

  .hero-description {
    line-height: 1.6;
    margin-bottom: 2rem;
    animation: fadeInUp 0.8s ease-out 0.4s both;
  }

  .btn-primary,
  .btn-secondary {
    padding: 1rem 2rem;
    border: none;
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
  }

  .btn-primary {
    background: linear-gradient(45deg, #00f5ff, #ff00ff);
    color: white;
    box-shadow: 0 4px 15px rgba(0, 245, 255, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 245, 255, 0.4);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(10px);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
  }

  .hero-stats {
    display: flex;
    gap: 2rem;
    animation: fadeInUp 0.8s ease-out 0.8s both;
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    display: block;
    font-size: 2.5rem;
    font-weight: bold;
    /* color: var(--color-accent); */
  }

  .stat-label {
    font-size: 1rem;
    opacity: 0.8;
  }

  .hero-visual {
    background-image: url("../../assets/hero/logo_bg.webp");
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    position: relative;
    width: 100%;
    height: 500px;
  }

  .products-showcase {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .product-card {
    position: absolute;
  }
  .card-1 {
    left: 40%;
    top: -40px;
    animation: floatCard1 6s ease-in-out infinite;
  }
  .card-2 {
    top: 19%;
    left: 15px;
    animation: floatCard2 6s ease-in-out infinite;
  }
  .card-3 {
    top: 20%;
    right: 65px;
    animation: floatCard3 8s ease-in-out infinite;
  }
  .card-4 {
    bottom: 8%;
    left: 65px;
    animation: floatCard4 7s ease-in-out infinite;
  }
  .card-5 {
    bottom: 12%;
    right: 100px;
    animation: floatCard5 5s ease-in-out infinite;
  }

  @keyframes floatCard2 {
    0%,
    100% {
      transform: translateY(0) rotate(-18deg);
    }
    50% {
      transform: translateY(-8px) rotate(-20deg);
    }
  }

  @keyframes floatCard3 {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-10px) rotate(-2deg);
    }
  }

  @keyframes floatCard4 {
    0%,
    100% {
      transform: translateY(0) rotate(-16deg);
    }
    50% {
      transform: translateY(-6px) rotate(-18deg);
    }
  }

  @keyframes floatCard5 {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-12px) rotate(-3deg);
    }
  }

  @keyframes floatCard1 {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-14px) rotate(3deg);
    }
  }

  .card-icon {
    width: 100px;
    height: auto;
  }

  @keyframes flowRight {
    0%,
    100% {
      opacity: 0.5;
    }
    50% {
      opacity: 1;
      transform: translateX(5px);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-20px) rotate(180deg);
    }
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .card-1 {
      left: 50px;
      bottom: 210px;
    }
    .card-2 {
      left: 120px;
      bottom: 70px;
    }
    .card-3 {
      top: 75px;
      right: 120px;
    }
  }
  @media (max-width: 1023px) {
    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: center;
    }

    .hero-stats {
      justify-content: center;
    }
  }

  @media (max-width: 425px) {
    .stat-number {
      font-size: 1.5rem;
    }

    .stat-label {
      font-size: 0.8rem;
    }
  }
</style>
